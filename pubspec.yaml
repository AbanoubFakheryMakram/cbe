name: cbe
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  # Service locator
  get_it: 7.2.0
  # Functional programming thingies
  dartz: ^0.10.0
  # Value equality
  equatable: ^2.0.3
  # Local storage
  flutter_secure_storage: ^5.0.2
  # State management
  flutter_riverpod: ^1.0.3
  # API requests
  dio: ^4.0.0
  pretty_dio_logger: ^1.1.1
  # connectivity
  internet_connection_checker: ^0.0.1+3
  # Icons
  font_awesome_flutter: ^9.1.0
  # Responsive framework
  # Network Images
  cached_network_image: ^3.2.0
  # Date Formatting
  intl: 0.17.0
  # Permissions
  permission_handler: ^8.2.5
  # Biometric auth
  local_auth: ^1.1.7
  # safe_device is used to check if the current device is Jailbroken, rooted, emulator and mock location detection.
  safe_device: ^1.1.0
  # Notifications
  awesome_notifications: ^0.6.17
  # Paths
  path_provider: ^2.0.4
  # Encryption
  aes_crypt_null_safe: ^2.0.2
  # Webview
  flutter_inappwebview: ^5.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  generate: true
  uses-material-design: true

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic

